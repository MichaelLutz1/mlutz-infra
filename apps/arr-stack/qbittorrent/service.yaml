apiVersion: v1
kind: Service
metadata:
  name: qbittorrent-service
  namespace: arr-stack
spec:
  type: ClusterIP
  selector:
    app: qbittorrent
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: webui
