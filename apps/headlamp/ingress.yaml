apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: headlamp-lan
  namespace: arr-stack
  annotations:
    gethomepage.dev/description: Cluster management UI for Kubernetes.
    gethomepage.dev/enabled: "true"
    gethomepage.dev/group: Cluster Management
    gethomepage.dev/icon: headlamp.png
    gethomepage.dev/name: Headlamp
    gethomepage.dev/pod-selector: "app.kubernetes.io/name=headlamp"
spec:
  ingressClassName: traefik
  rules:
    - host: headlamp.home.arpa
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: headlamp
                port:
                  number: 80
