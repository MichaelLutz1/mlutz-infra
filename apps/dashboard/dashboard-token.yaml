apiVersion: v1
kind: Secret
metadata:
  name: dashboard-login-token
  namespace: kubernetes-dashboard
  annotations:
    kubernetes.io/service-account.name: kubernetes-dashboard-web
type: kubernetes.io/service-account-token
