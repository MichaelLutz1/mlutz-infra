apiVersion: traefik.containo.us/v1alpha1
kind: TLSStore
metadata:
  name: default
  namespace: mlutz-infra   # must match Traefik’s namespace
spec:
  defaultCertificate:
    secretName: michaellutz-org-tls
